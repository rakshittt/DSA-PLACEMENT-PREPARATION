#include <bits/stdc++.h>
using namespace std ;

class Node{

    int data;
    Node* next;

    Node(int data){
        this->data = data;
        this->next =NULL;
    }
};

int main(){

    // Approach 1 : 
            // 1. Create a new LL = Reversed(LL);
            // 2. Then compare both LL and return result in Boolean form 
            // TC ->O(N) & SC -> O(N)
}